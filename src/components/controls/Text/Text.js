import styled from "styled-components";

const HeaderText = styled.span`
  color: red;
  font-size: 1em;
  justify-content: center;
`;

const FooterText = styled.span`
  color: red;
  font-size: 1em;
`;

const ProductsText = styled.span`
  color: red;
`;

export { HeaderText, FooterText, ProductsText };
